.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
artifacts/*
# Route C evidence baselines and gate decisions are committed deliberately
# (immutable dated evidence)
!artifacts/baseline/
!artifacts/gate0/
!artifacts/plan2/
!artifacts/plan3/
!artifacts/plan4/

# Data generated locally (anchored to repo root so it does not also ignore
# dataset/data/, which carries the committed tiny release tree)
/raw/
/data/
*.duckdb
*.duckdb.wal
*.parquet

# Dataset release candidate: the tiny tree is committed; the standard profile's
# large data is built on demand and uploaded, never committed.
dataset/data/standard/
dataset/truth/standard-manifest.json

# dbt build artifacts
dbt_neobank/target/
dbt_neobank/dbt_packages/
dbt_neobank/logs/
dbt_neobank/.user.yml

# Notebook runtime caches
notebooks/__marimo__/

# Local app/editor files
.env
.env.*
!.env.example
.vscode/
.idea/
Thumbs.db
.DS_Store

# internal working docs — kept local, not for the public repo
/NEOBANK_GCP_UPGRADE_KIT.md
/docs/PRODUCT_READY_PLAN.md
